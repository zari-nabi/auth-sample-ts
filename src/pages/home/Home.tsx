import Container from "../../components/Container/Container"

//=======================================
/**
 * The Home component displays the first page view of the application. A sample webpage based on Authentication.
 * @returns {JSX.Element} The Home component. 
 */
//=========================================
const Home = ()=>{
    return (
        <Container>
            <h1>sample of Authentication</h1>
        </Container>
    )
};

export default Home;